export function recentDeliveryPropsGetter(
	params: Grid.CellRenderer.Params
): Grid.CustomComponents.RecentDeliveryProps {
	const value = params.value;
	return value as Grid.CustomComponents.RecentDeliveryProps;
}
